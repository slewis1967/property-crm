import CreditAuthorisationClient from "./CreditAuthorisationClient";

export const dynamic = "force-dynamic";

/** Credit File Authorisation — list of authorisations, and the entry point for a new one. */
export default function CreditAuthorisationPage() {
  return <CreditAuthorisationClient />;
}
