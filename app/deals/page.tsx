import DealsClient from "./DealsClient";

export const dynamic = "force-dynamic";

/** Development Watchlist — captured site opportunities tracked through a pipeline. */
export default function DealsPage() {
  return <DealsClient />;
}
