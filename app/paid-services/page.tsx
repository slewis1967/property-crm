import PaidServicesClient from "./PaidServicesClient";

export const dynamic = "force-dynamic";

/** Paid Accounts — register of paid services + what needs attention today. */
export default function PaidServicesPage() {
  return <PaidServicesClient />;
}
