import FactFindClient from "./FactFindClient";

export const dynamic = "force-dynamic";

/** Borrower Fact Find — list of fact finds, and the entry point for a new one. */
export default function FactFindPage() {
  return <FactFindClient />;
}
