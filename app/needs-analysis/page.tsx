import NeedsAnalysisClient from "./NeedsAnalysisClient";

export const dynamic = "force-dynamic";

/** NCCP Client Needs Analysis — list of analyses, and the entry point for a new one. */
export default function NeedsAnalysisPage() {
  return <NeedsAnalysisClient />;
}
